---
- name: Play options- limit
  hosts: all
  gather_facts: false
  vars:
    ansible_python_interpreter: /home/naima/py_venv/venv1/bin/python3.12
  tasks: 
    - name: Print hostname
      ansible.builtin.debug:
        msg: "{{inventory_hostname}}"
    
    - name: Print hello
      ansible.builtin.debug:
        msg: "Hey there"